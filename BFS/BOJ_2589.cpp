#include "bits/stdc++.h"
using namespace std;
#define X first
#define Y second 

vector<string> board(52);
int dist[52][52];
int R, C;

void bfs(

int main(void) 
{
    ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> R >> C;
	for (int i=0; i<R; i++) 
	    cin >> board[i];
	
}
