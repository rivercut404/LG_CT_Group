#include "bits/stdc++.h"
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    stack<char> password;
    vector<char> v;

    // Record whether each character is cons or vow (T/F)
    // If there is no vow, CUT
    // If there are 3 contiguous type, CUT

}