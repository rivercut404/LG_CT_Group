#include "bits/stdc++.h"
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N;
    int min_time = 0;
    cin >> N;
    vector<int> P (N);

    for (int i=0; i<N; i++) 
        cin >> P[i];
    
    sort(P.begin(), P.end());

    for (int i=0; i<N-1; i++) {

    }
}