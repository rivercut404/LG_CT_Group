#include "bits/stdc++.h"
using namespace std;

int rain_t[101][101];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int H, W;

    cin >> H;
    cin >> W;

    for (int i=0; i<H; i++) {
        for (int j=0; j<W; j++) {
            char is_cloud;
            cin >> is_cloud;
        }
    }
}