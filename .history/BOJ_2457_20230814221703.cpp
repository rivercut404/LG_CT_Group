#include "bits/stdc++.h"
using namespace std;

int date_to_dec(pair<int,int> date) {
	if (date.first <= 2) return 0;
	else {
		
	}
}

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int N; cin >> N;
	
}
