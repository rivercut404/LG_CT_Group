#include "bits/stdc++.h"
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int layer[5000];

    for (int i=0; i<5000; i++) {
        layer[i] = (int)(i*(i+1))/2;
    }

    int X, layer_x;
    cin >> X;

    for (int i=1; )
}